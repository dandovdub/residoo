node_modules/
*.log
.DS_Store
# Internal launch/positioning notes — not part of the public repo.
LAUNCH.md
# Local test vaults must never be committed.
residoo-vault-*/
pypi/dist/
pypi/*.egg-info/
# Built locally or by CI (.github/workflows/publish.yml); the source
# (build.sh, scripts/postinstall) is committed, the binary is not.
packaging/macos-pkg/*.pkg
